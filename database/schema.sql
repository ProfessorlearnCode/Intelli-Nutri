-- schema.sql - SQLite Database Schema

-- Define tables here:
-- users (id, name, email, preferences_json)
-- recipes (id, title, ingredients, steps, nutrition, cuisine, embedding_vector)

-- This script will be run via init_db.py
